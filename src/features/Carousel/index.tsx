import React from 'react'; 
 import cn from 'classnames';
interface Props {
    
}


const Carousel:React.FC<Props> = React.memo(({}) => {
     return <div className={cn('w-full h-full')}> Carousel </div>
})
export default Carousel