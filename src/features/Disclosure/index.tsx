import React from "react";
import cn from "classnames";
interface Props {}
const DisclosureComponent: React.FC<Props> = React.memo(({}) => {
  return <div className={cn("")}> Disclosure </div>;
});
export default DisclosureComponent;
