import React from "react";
import cn from "classnames";
interface Props {}
const FormSelect: React.FC<Props> = React.memo(({}) => {
  return <div className={cn("")}> FormSelect </div>;
});
export default FormSelect;
