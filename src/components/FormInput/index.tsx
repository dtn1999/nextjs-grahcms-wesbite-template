import React from "react";
import cn from "classnames";
interface Props {}
const FormInput: React.FC<Props> = React.memo(({}) => {
  return <div className={cn("")}> FormInput </div>;
});
export default FormInput;
