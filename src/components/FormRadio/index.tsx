import React from "react";
import cn from "classnames";
interface Props {}
const FromRadio: React.FC<Props> = React.memo(({}) => {
  return <div className={cn("")}> FromRadio </div>;
});
export default FromRadio;
